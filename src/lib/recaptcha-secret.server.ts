/**
 * reCAPTCHA v3 — SECRET KEY (server only, never commit a real value)
 *
 * Leave this empty in GitHub. Paste the secret only on the VPS in
 * recaptcha-verify.php
 */
export const RECAPTCHA_SECRET_KEY = "";
